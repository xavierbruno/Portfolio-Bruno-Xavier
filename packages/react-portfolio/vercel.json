{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter react-portfolio build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
